apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shop-qa

resources:
  - ../../base
  - billing-secretproviderclass.yaml
  - vault-secretstore.yaml

patches:
  - path: orders-config-patch.yaml
  - path: orders-env-patch.yaml
